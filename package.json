{
  "name": "read",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "yarn run lint && yarn run react-scripts start",
    "build:start": "react-scripts build && yarn start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "lint": "./node_modules/.bin/eslint src/**/*.js"
  },
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.2",
    "eslint": "^4.18.2",
    "eslint-plugin-react": "^7.7.0",
    "prop-types": "^15.6.1"
  },
  "engines": {
    "node": ">= 8.9.0",
    "npm": ">= 5.5.1",
    "yarn": "1.3.2"
  }
}
